<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="180dp">

    <ImageView
        android:id="@+id/img_banner"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent" 
        android:src="@drawable/mask_dark"/>
    
    <ImageView
        android:id="@+id/img_usr"
        android:layout_width="@dimen/header_img"
        android:layout_height="@dimen/header_img"
        android:layout_alignBottom="@id/img_banner"
        android:layout_margin="@dimen/padding_small"/>

    <TextView
        android:id="@+id/txt_usr"
        style="@style/Base.TextAppearance.AppCompat.Display1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toRightOf="@id/img_usr"
        android:layout_alignBottom="@id/img_usr"/>
    
</RelativeLayout>